for module in compctl complete complist computil datetime deltochar mathfunc parameters terminfo ; do
  zmodload -i zsh/${module} 2>/dev/null || print "no ${module} found"
done && builtin unset -v mod
